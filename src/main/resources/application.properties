spring.application.name=ListeCourse

# Connexion au conteneur Docker
spring.datasource.url=jdbc:mysql://localhost:3306/listecourse
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate : crée les tables automatiquement
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Préfixe de l'API (comme ton projet Todo)
server.servlet.context-path=/api